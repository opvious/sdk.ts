{
  "name": "@opvious/template-protos",
  "version": "0.1.0",
  "repository": "github:opvious/template.ts",
  "description": "Proto definitions",
  "keywords": [
    "protos"
  ],
  "author": "Opvious engineering <eng@opvious.ai>",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=16"
  },
  "main": "./lib",
  "files": [
    "lib/",
    "resources/"
  ],
  "scripts": {
    "build": "tsc -p src",
    "clean": "rm -rf lib node_modules src/*.ts",
    "gen": "pnpm exec opvious-generate-ts-from-protos",
    "prepare": "pnpm run gen && pnpm run build"
  },
  "dependencies": {
    "@protobuf-ts/runtime": "^2.6.0",
    "@protobuf-ts/runtime-rpc": "^2.6.0"
  },
  "peerDependencies": {
    "@grpc/grpc-js": "^1.6.2"
  },
  "devDependencies": {
    "@grpc/grpc-js": "*",
    "@opvious/stl-grpc": "^0.5.28",
    "@protobuf-ts/plugin": "^2.6.0",
    "@types/node": "^16.11.7",
    "typescript": "^4.1.5"
  }
}
